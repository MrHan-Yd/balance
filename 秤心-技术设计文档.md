# ⚖️ 秤心 Balance — 买菜语音自动加和助手 · 技术设计文档

> **项目定位**：一款专为买菜/购物场景打造的单手极简、离线运行、无键盘、无像素感的高颜值 iOS/Android 跨平台语音累加计算器。

---

## 0. 文档信息

| 项 | 值 |
| :--- | :--- |
| 版本 | v0.5.0（设计评审版） |
| 更新日期 | 2026-08-04 |
| 状态 | 待评审 |
| 作者 | — |

### 修订记录

| 版本 | 日期 | 修订内容 |
| :--- | :--- | :--- |
| v0.1.0 | — | 初稿：定位、技术栈、架构、UI 规范、iOS 免签方案、里程碑 |
| v0.2.0 | 2026-08-04 | 修复数据模型与解析器单数字问题；定义交互语义、单位映射、错误兜底、会话/历史、测试策略、风险与对策、验收标准、App 基础信息 |
| v0.3.0 | 2026-08-04 | 应用定名"秤心 Balance"；配色统一为琥珀金×石墨黑暗色语言（§4.6、§5.3）；UI 原型暗色化（ui/index.html v0.3）；图标定稿（ui/icon.svg v4） |
| v0.4.0 | 2026-08-04 | 配色定稿为科技蓝×深空（§4.6、§5.3）：电光蓝发光 #E3F2FF→#4FC3F7→#1565C0，深空蓝黑底 #0B1220→#070B14；ui/icon.svg v5、ui/index.html v0.4 同步 |
| v0.5.0 | 2026-08-04 | 配色降饱和定稿（60-30-10 法则）：中性深灰蓝底 #0E131A→#080B10，沉稳蓝 #2E5C96→#1B3A66 / #2F6FCC→#1C4E8E，电光蓝仅作 10% 点缀；ui/icon.svg v6、ui/index.html v0.5 同步 |

---

## 1. 业务需求与场景分析

* **核心痛点**：菜市场买菜时双手被占用（拿菜/提袋），传统计算器需要频繁手动输入数字，操作繁琐且易按错；免费 iOS 安装包面临 7 天过期问题。
* **目标用户**：菜市场/夜市/水果摊高频购物的中青年用户，习惯单手操作，iPhone（走 iOS 18 免签渠道）与 Android 手机各占一半。
* **核心场景**：
  1. 用户点击/按住大语音按钮，说出“五块五”、“38”或“豆腐 12.5”。
  2. 手机自动调用系统本地语音引擎识别文本，解析器提取出浮点数值（`5.5`、`38`、`12.5`）。
  3. 界面实时平滑动画更新当前总价，并插入明细列表（显示商品名/原文 + 金额）。
  4. 支持单手一键“撤销上一笔”或“全部清空”。
* **交互语义（v0.2 定稿）**：
  | 操作 | 行为 |
  | :--- | :--- |
  | **点击** | 单次识别：开始录音，识别出首个有效金额（或 3 秒超时）后自动停止并累加 |
  | **按住** | 持续识别：按住期间连续识别并逐条累加，松手停止 |
  | 识别中 | 按钮显示声波脉冲动画；识别成功轻微震动 + 数字滚动动画 |
  | 识别失败 | 重震动 + 提示“没听清，请重说”，不产生任何累加 |
* **边界场景**：
  | 场景 | 期望行为 |
  | :--- | :--- |
  | 一句含多个金额（“黄瓜5块，牛肉38”） | 逐条插入明细并分别累加 |
  | 识别错误/说错 | 明细行可滑动删除，或一键撤销上一笔 |
  | 麦克风/识别权限被拒 | 降级为手动数字小键盘输入模式（不阻断使用） |
  | 无明细（空状态） | 显示引导文案“开始说话，自动记账” |
  | 噪音过大、无有效识别结果 | 提示重说，不累加；连续失败 2 次建议使用手动键盘 |
* **网络与服务依赖**：**零后端服务（0 Server）**，纯本地运行，不调用收费云端 API，不依赖翻墙环境。

---

## 2. 技术栈选型 (Tech Stack)

| 模块 | 选型 | 说明 |
| :--- | :--- | :--- |
| **应用框架** | **Flutter 3.x (Dart)** | 矢量级 GPU 渲染（Impeller），完美消除像素感，原生机器码极速响应。 |
| **语音识别** | `speech_to_text` (插件) | 直连 iOS `SFSpeechRecognizer` / Android `SpeechRecognizer` 本地识别引擎。 |
| **状态管理** | **Riverpod**（Notifier + Provider） | 定稿为单一方案，避免 setState 散落；便于解析器/会话逻辑单测。 |
| **本地数据存储** | **hive** | 结构化保存当前会话与历史购物记录（列表 + 类型安全），不引入重量级 DB。 |
| **UI 矢量字体** | SF Pro / Google Fonts (`Montserrat`) | 高清晰度等宽数字字体，提升界面精致感。 |
| **测试** | `flutter_test`（单元 + Widget） | 解析器用例表、会话状态机、明细增删撤。 |
| **iOS 免签发布** | **SideStore + WireGuard** | 本地环路（Loopback）自动续签，iOS 18 零成本长期稳定使用。 |

---

## 3. 架构设计与数据流

整体架构采用 **极简 MVVM 模式（Riverpod）**，所有数据流动均在手机内存与本地存储中完成：

```text
 [用户语音输入] (点击/按住)
       │
       ▼
 [iOS/Android 本地原生语音引擎] (speech_to_text)
       │ (返回识别字符串, 如: "牛肉 38 块 5")
       ▼
 [文本解析与数字提取器] (RegExp + 中文数字/单位映射)
       │ (提取金额列表: [38.5])
       ▼
 [会话状态机] (Riverpod Notifier: SessionState)
       ├──> 追加明细 → 更新总价 → 写入 hive 持久化
       ├──> 撤销栈 push/pop
       └──> 触发平滑数字滚动与波纹动画 (Impeller)
       │
       ▼
 [UI 视图渲染] (矢量毛玻璃卡片 + 高清大字显示)
```

**错误处理流**：识别失败 / 解析为空 / 权限被拒 → 不进入状态机，直接走提示 → 手动键盘兜底。

---

## 4. 关键功能模块设计

### 4.1 数据模型 (Data Model)

```dart
/// 购物明细条目
class CartItem {
  final String label;      // 原始识别文本片段（如 "豆腐"）；纯数字时为空
  final double amount;     // 金额（元），两位小数精度
  final DateTime addedAt;  // 添加时间
}

/// 当前会话状态
class SessionState {
  final List<CartItem> items;      // 明细列表（上限 200 条，超出最旧归档到历史）
  final List<CartItem> undoStack;  // 撤销栈
  double get total;                // 总额 = Σ amount
}
```

> 明细行 UI 展示 = `label`（可空）+ `amount` 格式化（`¥1,234.56`）+ 时间。**不再使用 `List<double>` 纯数值模型**。

### 4.2 文本解析与数字提取算法 (Text Parser)

**解析管线（v0.2 重写，支持单句多金额）**：

```text
输入文本
  │ ① 中文数字归一化:  "牛肉三十八" → "牛肉38"
  │    数字表: 零0 一1 二/两2 三3 四4 五5 六6 七7 八8 九9
  │    单位表: 十×10 百×100 千×1000 万×10000（支持组合: "三十八"→38, "一万零五"→10005）
  ▼
  │ ② 金额单位映射（只认金额单位，重量/数量单位忽略）:
  │    块/元 → 整数部分结束（"38块5" → "38.5"）
  │    毛/角 → 小数点后 1 位（"五块五毛" → "5.5"）
  │    分    → 小数点后 2 位（"九块九毛九" → "9.99"）
  ▼
  │ ③ 多金额提取: 正则扫描全部候选数字（非 firstMatch）
  ▼
  │ ④ 过滤规则:
  │    金额单位相邻的数字 → 提取（"黄瓜5块" → 5）
  │    重量/数量单位（斤/两/个/袋/根）相邻的数字 → 忽略（"三斤"不提取）
  │    孤立数字（"38"）→ 提取（视为直接报价）
  │    数值 ≤ 0 或 > 99999 → 无效
  ▼
 返回 List<PriceMatch>（逐个插入明细）
```

```dart
class PriceMatch {
  final double value;    // 提取的金额
  final String rawText;  // 原文片段，作为明细 label
}

List<PriceMatch> parseVoiceTexts(String input) {
  String normalized = normalizeChineseNumbers(input);   // ①
  normalized = mapCurrencyUnits(normalized);            // ②
  // ③④ 扫描全部匹配 + 过滤，返回列表
}
```

**已知限制**：`"三斤5块"`（单价×重量）这类复合语义不计算，只认总价表达；方言/口音识别能力依赖系统引擎（zh-CN），不属于本应用可控范围。

### 4.3 语音识别会话管理 (Speech Session)

* 状态机：`idle → listening → (success | failed | timeout) → idle`
* 单次识别超时 **3 秒**；持续识别无结果 **8 秒**自动停止并提示。
* 识别失败（`noMatch` / `notRecognized` / 引擎 busy）→ `HapticFeedback.heavyImpact` + 提示“没听清，请重说”。
* 权限被拒（首次弹窗引导授权；再次拒绝）→ 底部语音键切换为**数字小键盘模式**，支持 0-9/小数点/退格/确认，与语音模式同样走会话状态机。
* 应用进入后台 → 立即停止识别，防止误触发。

### 4.4 会话与历史记录 (Session & History)

* **当前会话**：内存中的 `SessionState`，App 重启后从 hive 自动恢复。
* **历史记录**：App 内“历史”页，按天分组（日期、条目数、总额），可查看/清空/删除单日。
* **持久化策略**：hive 存储，历史保留最近 **30 天**自动清理；会话明细超 200 条时最旧条目归档入历史。

### 4.5 撤销与清空 (Undo & Clear)

* **撤销**：弹回 `undoStack` 栈顶，即撤销最近一次累加（覆盖“说错重说”场景）；可连续撤销。
* **清空**：需二次确认（底部弹层“确认清空全部？”），清空后 `undoStack` 同步清空。

### 4.6 视觉与 UI/UX 规范 (High-Precision Design)

为确保界面**极具精致感、零像素感**，遵循以下设计规范（单位均为逻辑像素 dp）：

1. **色彩搭配**（v0.5 定稿，60-30-10 法则：中性 60% / 沉稳蓝 30% / 电光蓝点缀 10%）：
   * 背景色：中性深灰蓝渐变（`#0E131A` → `#080B10`），电光蓝弥散光斑仅作弱点缀。
   * 主色调/总价卡片：沉稳蓝灰渐变（`#2E5C96` → `#1B3A66`），弱蓝色辉光 + 顶部暖白高光线（`BlurRadius: 20`）。
   * 录音按钮：沉稳蓝发光渐变（`#2F6FCC` → `#1C4E8E`），按住时触发红光呼吸动画（`AnimationController`）。
   * 语义色：识别失败/清空警示沿用红色（`#E53935`）；金额数字淡蓝（`#90CAF9`）与电光蓝斜切为仅有的高饱和点缀；主体文字暖白（`#E8F1FA`）。
2. **布局结构**（顶部 35% / 中部 45% / 底部 20%）：
   * **顶栏**：半透明毛玻璃总价卡片，80dp 巨大高清数字展示（`¥` 符号 + 千分位，两位小数）。
   * **中栏**：平滑滚动的购物明细列表（名称 + 金额 + 时间），支持滑动删除单项；空状态显示引导文案。
   * **底栏**：占据全宽的巨型“按住/点击 说话”主按键（便于盲操），识别中显示声波脉冲动画。
3. **动效**：金额变化用数字滚动动画（`AnimatedSwitcher` + 位数滚动，自实现，不引第三方库）；识别成功 `HapticFeedback.lightImpact`。
4. **无障碍**：适配 VoiceOver / TalkBack（每次累加播报金额）；支持系统字体缩放。

---

## 5. 权限与平台配置 (Permissions & Native Setup)

### 5.1 iOS 配置 (`ios/Runner/Info.plist`)

```xml
<key>NSMicrophoneUsageDescription</key>
<string>秤心需要使用麦克风以听取金额数字</string>
<key>NSSpeechRecognitionUsageDescription</key>
<string>秤心需要使用系统语音识别将话语转为金额数字</string>
```

* 权限被拒时：首次弹出引导授权说明页；再次拒绝 → 自动切换手动键盘模式。

### 5.2 Android 配置 (`android/app/src/main/AndroidManifest.xml`)

```xml
<uses-permission android:name="android.permission.RECORD_AUDIO"/>

<!-- Android 11+ 包可见性：部分机型需声明识别服务查询，否则识别返回空 -->
<queries>
  <intent>
    <action android:name="android.speech.RecognitionService" />
  </intent>
</queries>
```

* **运行时权限**：Android 6.0+ 需运行时动态申请（由 `speech_to_text` 请求，应用侧处理拒绝回调 → 切换手动键盘模式）。

### 5.3 App 基础信息 (App Meta)

| 项 | 值（暂定） |
| :--- | :--- |
| App 名称 | 秤心 Balance |
| 包名 / Bundle ID | `com.chengxin.balance` |
| 最低系统版本 | iOS 15.0+ / Android 8.0 (API 26) |
| 语言 | 简体中文（zh-CN 识别 locale） |
| 图标 | `ui/icon.svg`（v6 定稿：中性深灰蓝底 + 倾斜切割胶囊 + 电光蓝发光斜切） |
| 启动页 | Phase 2 内产出 |

---

## 6. iOS 18 免签部署与长期续签方案

为了在不支付苹果 $99/年 开发者费用的前提下，在 iOS 18 设备上实现永久稳定运行，部署流程如下：

```text
[Mac 编译 Flutter 应用]
   │  运行: flutter build ipa --release
   ▼
[获得未签名包 / 打包包] -> 导入 iPhone
   │
   ▼
[iPhone SideStore 首次安装]
   │  在 WireGuard 中激活 SideStore.conf 本地环路网卡
   ▼
[iOS 快捷指令自动化 (Shortcuts)]
   ├── 触发时间：每周一/周四 03:00 (设定为“立即运行”)
   ├── 步骤 1: 开启 WireGuard VPN (127.0.0.1 闭环)
   ├── 步骤 2: 触发 SideStore 隐密刷新
   └── 步骤 3: 关闭 WireGuard VPN
   │
   ▼
[结果：剩余有效期保持 6~7 天，零闪退，零电脑依赖]
```

**失败降级方案（v0.2 补充）**：
* 快捷指令自动化在锁屏/后台可能被系统拦截（“立即运行”需授权）→ 应用内提供**手动一键刷新**入口（设置页），并推送每周提醒通知。
* 证书到期未能刷新 → 应用闪退时展示引导页（重新打开 SideStore 刷新）。
* iOS 大版本更新导致 SideStore 失效 → 关注社区兼容方案，降级到电脑重签（AltStore/Apple Configurator）。

---

## 7. 测试策略 (Testing Strategy)

| 层级 | 内容 | 验收 |
| :--- | :--- | :--- |
| **单元测试** | 解析器用例表 ≥ 40 例：纯数字、中文数字组合（含“两/万/零”）、单位映射（块/毛/角/分）、单句多金额、无效输入（纯重量、超范围、乱语）、边界（0、99999、1.99 精度） | 全绿 |
| **单元测试** | 会话状态机：累加、撤销栈、清空确认、200 条上限归档、重启恢复 | 全绿 |
| **Widget 测试** | 明细增删、撤销/清空交互、空状态、手动键盘输入流 | 全绿 |
| **真机验证** | iOS 18 + Android 8.0 低端机各 1 台：噪音环境识别、权限拒绝流、后台恢复、连续识别稳定性 | 清单逐项通过 |

---

## 8. 风险与对策 (Risks & Mitigations)

| 风险 | 影响 | 对策 |
| :--- | :--- | :--- |
| 菜市场噪音大，识别率低 | 核心体验受损 | 手动键盘兜底 + 撤销重说 + 持续识别模式；连续失败 2 次引导切键盘 |
| 方言/口音导致识别错误 | 累加结果错误 | 明细行显示原文 + 可撤销；提示语速放慢 |
| 金额解析歧义（“5块5” vs “55”） | 累加错误 | 单位映射规则固定（块=小数点分隔），明细显示原文便于核对 |
| iOS 免签 7 天证书失效 | 应用闪退 | 自动化刷新 + 失败提醒 + 应用内手动刷新入口（见 §6） |
| iOS 系统大版本更新破坏 SideStore | 无法续签 | 关注社区方案，降级电脑重签 |
| 长按误触持续识别 | 误累加 | 持续识别模式需按住 ≥ 300ms 才激活 |

---

## 9. 项目里程碑规划 (Roadmap)

* [ ] **Phase 1 (基础原型)**：搭建 Flutter 项目结构，引入 `speech_to_text` 与 Riverpod，实现解析器。
  * ✅ **验收**：解析器单测 ≥ 40 例全绿；真机冷启动到可说话 < 1s。
* [ ] **Phase 2 (高质 UI 绘制)**：毛玻璃总价卡片、动效语音按钮、数字滚动动画与字体优化。
  * ✅ **验收**：120Hz 设备数字动画 ≥ 60fps 不掉帧；空状态/无障碍可用。
* [ ] **Phase 3 (功能完善)**：撤销、单项滑动删除、音效与震动反馈（`HapticFeedback`）、手动键盘兜底。
  * ✅ **验收**：Widget 测试全绿；撤销/清空语义符合 §4.5。
* [ ] **Phase 4 (iOS 打包与自动化部署)**：Mac 构建包，iOS 18 真机配置 WireGuard + SideStore 自动化续签。
  * ✅ **验收**：SideStore 安装成功，连续 2 周自动刷新无人工干预。
* [ ] **Phase 5 (测试与验收)**：单元/Widget/真机清单全过（§7），识别用例通过率 ≥ 95%。
* [ ] **Phase 6 (发布与维护)**：侧载分发说明、问题反馈渠道、30 天历史与 hive 数据校验。
